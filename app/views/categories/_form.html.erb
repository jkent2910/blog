<%= form_for @category do |f| %>
 <div class="control-group">
   <%= f.label :name %>
   <div class="controls">
     <%= f.text_field :name %>
   </div>
 </div>

 <div class="form-actions">
   <%= f.submit nil %>
   <%= link_to 'Cancel', categories_path %>
 </div>
<% end %>